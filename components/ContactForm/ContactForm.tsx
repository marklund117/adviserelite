import { Button, Checkbox, Group, TextInput } from '@mantine/core'
import { useForm } from '@mantine/form'

export default function ContactForm() {
    return <div>This is a future form component.</div>
}

